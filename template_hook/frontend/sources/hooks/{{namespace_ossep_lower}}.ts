/**
 * Nom du module         : {{lower_name}}.ts
 * Description           : 
 * 
 * Auteur                : {{user_name}}
 * Date de création      : {{datetime_full}}
 * Dernière modification : {{datetime_full}}
 * Version               : 1.0.0
 * Licence               : {{licence_name}}
 * 
 * Notes                 : 
 */

import { useState, useEffect } from 'react';

/**
 * Paramètres du hook {{title_name}}.
 */
interface Use{{title_name}}Props {
  
}

/**
 * Valeur retournée par le hook {{title_name}}.
 */
interface Use{{title_name}}Return {
  
}

/**
 * Gère la logique du hook {{title_name}}.
 * 
 * @param {Use{{title_name}}Props} props Les paramètres du hook.
 * @returns {Use{{title_name}}Return} Les données et fonctions exposées par le hook.
 */
export default function use{{title_name}}({}: Use{{title_name}}Props): Use{{title_name}}Return {
  const [ value, setValue ] = useState('');

  useEffect(() => {
    
  }, [ ]);

  return {
    
  };
}
